module TestSameLength do
  export [test/0]

  # Function that requires same-length vectors
  def dot_product(v1: Vector(Float, n), v2: Vector(Float, n)): Float =
    42.0

  def test(): Unit =
    # Both vectors are explicitly length 3 
    let v1 = [1.0, 2.0, 3.0]
    let v2 = [4.0, 5.0, 6.0] 
    
    # This should work: Vector(Float, 3) + Vector(Float, 3)
    let result = dot_product(v1, v2)
    
    ok

end