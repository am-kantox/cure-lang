module AdvancedFeaturesTest do
  export [test/0]

  // Record definition - COMPLETED ✅
  record Person do
    name: String
    age: Number
  end

  def test() do
    // Lambda expressions - COMPLETED ✅
    let greeting_fn = fn(name) -> "Hello, #{name}!" end in
    
    // Record creation
    let person = Person{name: "Alice", age: 30} in
    
    // String interpolation - COMPLETED ✅
    let message = "Person: #{person.name}, Age: #{person.age}" in
    
    // Advanced pattern matching - COMPLETED ✅  
    let numbers = [1, 2, 3, 4, 5] in
    match numbers do
      [head | tail] -> greeting_fn(person.name)
      [] -> "empty"
      _ -> "other"
    end
  end
end