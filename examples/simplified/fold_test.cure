module FoldTest do
  export [demo/0]
  
  import Std.List [fold/3]
  
  def demo(): Int =
    let numbers = [1, 2, 3]
    fold(numbers, 0, add_func)
  
  def add_func(x: Int): Int -> Int = 
    fn(acc) -> x + acc end

end
