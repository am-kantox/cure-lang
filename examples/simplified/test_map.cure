module MapTest do
  export [demo/0]
  
  def demo(): Int =
    let numbers = [1, 2, 3]
    let f = double_func
    # Test that function references work for higher-order functions
    let result1 = f(1)
    let result2 = f(2) 
    let result3 = f(3)
    result1 + result2 + result3  # Should be 2 + 4 + 6 = 12
  
  def double_func(x: Int): Int = 
    x * 2

end
