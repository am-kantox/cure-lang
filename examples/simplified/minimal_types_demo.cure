def demo() do
  let numbers = [1, 2, 3, 4, 5] in
  
  let first = match numbers do
    [head | _] -> head
    [] -> 0
  end in
  
  let doubled = double_all(numbers) in
  
  let sum = first + 42 in
  
  // Return simple result
  sum
end

def double_all(list) do
  match list do
    [] -> []
    [head | tail] -> [head * 2 | double_all(tail)]
  end
end