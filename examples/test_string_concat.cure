# Test <> operator for string concatenation
module TestStringConcat do
  export [test/0]
  
  import Std.Io [println/1]
  
  def test(): Int =
    let s1 = "Hello" in
    let s2 = " " in
    let s3 = "World" in
    let result = s1 <> s2 <> s3 in
    println(result)
    0
end
