# Cure Standard Library - FSM Module
# Provides operations for working with FSMs

module Std.Fsm do
  export []

  type Payload = List(Pair(Atom, Any))
  type FsmName = Pair(Atom, Any)
  type Event = Pair(Atom, Payload)
  type State = Atom

  record FsmState do
    pid: Pid
    payload: Payload
    states: List(State)
    # transitions: List(Pair(State, Event))
  end

  ## Example
  

end
